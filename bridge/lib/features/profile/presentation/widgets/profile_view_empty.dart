import 'package:flutter/material.dart';

class ProfileViewEmpty extends StatelessWidget {
  const ProfileViewEmpty({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Profile not found'));
  }
}
